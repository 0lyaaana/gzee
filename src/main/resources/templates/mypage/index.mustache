{{>layout/header}}

<div class="container">
    <div class="row mt-5">
        <div class="col-md-3">
            {{>mypage/sidebar}}
        </div>
        <div class="col-md-9">
            <div class="card">
                <div class="card-header bg-primary text-white">
                    <h4>마이페이지</h4>
                </div>
                <div class="card-body">
                    {{#update}}
                        <div class="alert alert-success">
                            프로필이 성공적으로 업데이트되었습니다.
                        </div>
                    {{/update}}
                    {{#password}}
                        <div class="alert alert-success">
                            비밀번호가 성공적으로 변경되었습니다.
                        </div>
                    {{/password}}
                    <h5>{{user.nickname}}님, 환영합니다!</h5>
                    <p>이메일: {{user.email}} {{#user.emailVerified}}<span class="badge bg-success">인증됨</span>{{/user.emailVerified}}
                    {{^user.emailVerified}}<span class="badge bg-danger">인증 필요</span>{{/user.emailVerified}}</p>
                    <p>가입일: {{user.createdAt}}</p>
                    
                    <div class="row mt-4">
                        <div class="col-md-6">
                            <div class="card mb-3">
                                <div class="card-header">나의 확정 계획</div>
                                <div class="card-body">
                                    <a href="/mypage/plans" class="btn btn-outline-primary">확정 계획 보기</a>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="card mb-3">
                                <div class="card-header">나의 후기</div>
                                <div class="card-body">
                                    <a href="/mypage/reviews" class="btn btn-outline-primary">후기 보기</a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

{{>layout/footer}} 