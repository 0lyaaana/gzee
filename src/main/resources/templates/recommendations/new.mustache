{{>layout/header}}

<div class="container mt-4">
    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0">새 활동 추천하기</h4>
                </div>
                <div class="card-body">
                    {{#errorMessage}}
                        <div class="alert alert-danger">
                            {{errorMessage}}
                        </div>
                    {{/errorMessage}}
                    
                    <div class="alert alert-info">
                        여러분의 추천 활동이 목록에 추가됩니다. 재미있는 활동을 추천해 주세요!
                    </div>
                    
                    <form action="/recommendations/new" method="post">
                        <div class="mb-3">
                            <label for="title" class="form-label">활동 제목</label>
                            <input type="text" class="form-control" id="title" name="title" value="{{title}}" required>
                            {{#validationErrors.title}}
                                <div class="text-danger">{{.}}</div>
                            {{/validationErrors.title}}
                        </div>
                        
                        <div class="mb-3">
                            <label for="description" class="form-label">활동 내용</label>
                            <textarea class="form-control" id="description" name="description" rows="4" required>{{description}}</textarea>
                            <small class="form-text text-muted">어떤 활동인지 자세히 설명해주세요.</small>
                            {{#validationErrors.description}}
                                <div class="text-danger">{{.}}</div>
                            {{/validationErrors.description}}
                        </div>
                        
                        <div class="d-flex justify-content-between">
                            <a href="/recommendations" class="btn btn-secondary">취소</a>
                            <button type="submit" class="btn btn-primary">등록하기</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

{{>layout/footer}} 